clc; clear;
a = [[1,3];[2,5];[3,4];[4,3];[5,2];[5,1]];
%classes for data
M = mean(a);
a_new = a - M;
a_val = [4;2];
[W, y] =LDA(a_new, a_val);
mu_1 = mean(a_new(1:4, :));
mu_2 = mean(a_new(5:6, :));
slope_of_projection_line = (W(2,1) - W(1,1))/2;

l = W * mu_1

x = [-2.5, 2.5];
y = slope_of_projection_line * x;


h = figure;
scatter(a_new(:, 1), a_new(:, 2));
hold on
plot(x, y)
xlim([-2.5, 2.5])
ylim([-2.5, 2.5])
line([0 0], ylim);
line(xlim, [0 0]); 
hold off
waitfor(h)